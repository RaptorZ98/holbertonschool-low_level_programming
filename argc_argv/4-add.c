#include "main.h"
#include <stdio.h>

/**
 * main - adds positive numbers
 * @argc: parameter 1
 * @argv: parameter 2
 * Return: int
 */

int main(int argc, char **argv)
{
	int i;
	int m;

	if (argc < 1)
		printf("0\n");
	for(
		for (i = 0; i < argc; i++)
		{
			if ()
		}
}
