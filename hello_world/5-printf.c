#include <stdio.h>

/**
 * main - uses printf to print something
 * description - uses printf to print what was asked
 * Return: the correct retured value is 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
