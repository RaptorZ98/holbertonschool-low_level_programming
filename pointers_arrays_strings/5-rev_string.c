#include "main.h"

/**
 * rev-string - prints string in reverse
 * @s: parameter
 * Reutrn: void
 */

void rev-string(char *s)
{
	int a;
	int b;

	b = 0;
	for (a = 0; *(s + a) != '\0'; a++)
	{
	}
	for (a = a - 1; a >= 0; a--)
	{
		*(s + b) = 
		b++;
	}	
}
